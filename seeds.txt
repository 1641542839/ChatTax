# Seed URLs for ChatTax crawler
# One URL per line, lines starting with '#' are comments
# The crawler will follow links only within the same hostname and path prefix

# Example seeds (uncomment to use):
# https://www.ato.gov.au/individuals
# https://www.ato.gov.au/business

# Add your seed URLs below:
